import { styled } from "styled-components";

export const Nav = styled.nav`
  padding: 0 24px;
  display: flex;
  gap: 48px;
  width: 100%;
`;

export const NavLink = styled.a`
  margin: auto 0;
  font-weight: 500;
  cursor: pointer;
`;
